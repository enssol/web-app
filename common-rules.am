.POSIX:

# Standard rules
.PHONY: all clean distclean check

all:
	@echo "Building project..."

clean:
	rm -rf *.o *.tmp *.log

distclean: clean
	rm -rf Makefile config.status config.log

check:
	@echo "Running tests..."
